#!/usr/bin/env bash

if [[ -n "$1" && -f "$1" ]]; then
  ptyxis --tab --title "$(basename "$1") - " -x "emacsclient -nw -c \"$1\""
else
  ptyxis --tab --title "*scratch* - " -x "emacsclient -nw -c"
fi

