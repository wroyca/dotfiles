---@type LazyPluginSpec
return {
  [[mini.align]],
  config = true,
  keys = {
    { [[ga]], mode = { [[n]], [[v]] }, desc = [[Align]] },
    { [[gA]], mode = { [[n]], [[v]] }, desc = [[Align with preview]] }
  }
}
