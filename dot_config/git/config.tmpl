[user]
  name = {{ .name | quote }}
  email = {{ .email | quote }}

[credential]
  credentialStore=cache
  helper = {{.chezmoi.homeDir}}/.dotnet/tools/git-credential-manager

[init]
  defaultBranch = main

[pull]
  rebase = true

[rebase]
  autoStash = true
