# GDB commands to automatically execute during GDB startup.
#
# For more information, see the GDB manual with "info -f gdb -n Startup".

set confirm off
set verbose off
set history filename ~/.gdb_history
set history save
set history remove-duplicates 10

set disassembly-flavor intel

skip -gfi /usr/include/c++/*/*
skip -gfi /usr/include/c++/*/*/*
