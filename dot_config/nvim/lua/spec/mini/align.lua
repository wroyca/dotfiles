---@type LazyPluginSpec
return {
  [[mini.align]], config = true,

  keys = {
    { [[ga]], mode = { [[n]], [[x]] }, desc = [[Align text]] },
    { [[gA]], mode = { [[n]], [[x]] }, desc = [[Align text (with preview)]] }
  }
}
